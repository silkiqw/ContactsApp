import Foundation
import Contacts

enum UrlAddressType: String {
    case custom = "CUSTOM"
    case home = "HOME"
    case homepage = "HOMEPAGE"
    case other = "OTHER"
    case school = "SCHOOL"
    case work = "WORK"

    static func fromLabel(_ label: String?) -> UrlAddressType? {
        guard let label = label else {
            return nil
        }
        switch label {
        case CNLabelHome:
            return .home
        case CNLabelURLAddressHomePage:
            return .homepage
        case CNLabelOther:
            return .other
        case CNLabelSchool:
            return .school
        case CNLabelWork:
            return .work
        default:
            return .custom
        }
    }

    func toLabel() -> String? {
        switch self {
        case .home:
            return CNLabelHome
        case .homepage:
            return CNLabelURLAddressHomePage
        case .other:
            return CNLabelOther
        case .school:
            return CNLabelSchool
        case .work:
            return CNLabelWork
        default:
            return nil
        }
    }
}
