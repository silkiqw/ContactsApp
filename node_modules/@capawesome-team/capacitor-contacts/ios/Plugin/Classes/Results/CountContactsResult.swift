import Foundation
import Contacts
import Capacitor

@objc public class CountContactsResult: NSObject, Result {
    private let total: Int

    init(total: Int) {
        self.total = total
    }

    public func toJSObject() -> AnyObject {
        var result = JSObject()
        result["total"] = total
        return result as AnyObject
    }
}
