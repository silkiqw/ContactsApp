import Foundation
import Capacitor

@objc public class DisplayCreateContactResult: NSObject, Result {
    private let id: String

    init(id: String) {
        self.id = id
    }

    public func toJSObject() -> AnyObject {
        var result = JSObject()
        result["id"] = id
        return result as AnyObject
    }
}
