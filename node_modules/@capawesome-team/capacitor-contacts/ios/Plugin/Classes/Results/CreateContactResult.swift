import Foundation
import CoreBluetooth
import Capacitor

@objc public class CreateContactResult: NSObject, Result {
    private let id: String

    init(id: String) {
        self.id = id
    }

    public func toJSObject() -> AnyObject {
        var result = JSObject()
        result["id"] = id
        return result as AnyObject
    }
}
